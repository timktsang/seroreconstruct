% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_trajectory}
\alias{plot_trajectory}
\title{Plot antibody trajectory with model fit}
\usage{
plot_trajectory(
  fit,
  id = 1,
  subjects = NULL,
  n_samples = 100,
  main = NULL,
  col_infected = NULL,
  col_uninfected = NULL,
  show_legend = TRUE,
  ...
)
}
\arguments{
\item{fit}{A \code{seroreconstruct_fit} or \code{seroreconstruct_joint}
object.}

\item{id}{Row index (integer) or subject identifier. If \code{subject_ids}
was provided to \code{sero_reconstruct()}, or if \code{subjects} is
supplied here, the value is matched against those IDs. Otherwise treated
as a 1-based integer row index.}

\item{subjects}{Optional vector of subject identifiers aligned with fit
rows. Use when \code{subject_ids} was not provided at fitting time.
Example: \code{subjects = inputdata$subject_id}.}

\item{n_samples}{Number of posterior samples to draw. Default 100.}

\item{main}{Optional plot title. If \code{NULL}, a default title with the
individual index and posterior infection probability is generated.}

\item{col_infected}{Color for infected trajectories. Default semi-transparent
red.}

\item{col_uninfected}{Color for uninfected trajectories. Default
semi-transparent blue.}

\item{show_legend}{Logical; whether to draw a legend. Default \code{TRUE}.}

\item{...}{Additional graphical parameters passed to \code{plot()}.}
}
\value{
Invisible \code{NULL}. Called for its side effect of producing a plot.
}
\description{
For a single individual, draws posterior-sampled antibody trajectories
overlaid on observed HAI titers. Red lines show trajectories where infection
occurred; blue lines show trajectories without infection. Matches the
visualization style of Figure 1B in Tsang et al. (2022).
}
\examples{
\donttest{
fit <- sero_reconstruct(inputdata, flu_activity,
                        n_iteration = 2000, burnin = 1000, thinning = 1)
plot_trajectory(fit, id = 1)
}
}
